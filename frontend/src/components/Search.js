const Search = () => {
  return (
    <input
      type="text"
      placeholder="Search"
      id="searchbar"
    //   onChange={handleChange}
    //   value={searchInput}
    />
  );
};

export default Search;
