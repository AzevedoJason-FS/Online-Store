import Search from "./Search";
import Cart from "./Cart";

const Topbar = () => {
  return (
    <div className="top-bar">
      <Search />
        <Cart />
    </div>
  );
};

export default Topbar;
